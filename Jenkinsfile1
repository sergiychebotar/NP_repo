pipeline{
    agent any
    options{
        buildDiscarder(logRotator(numToKeepStr: '5', daysToKeepStr: '5'))
        timestamps()
    }
    environment {
        MYSQL_USER = "your_wordpress_database_user"
        MYSQL = credentials('MYSQL')
    }
    stages{
       stage('Deploying image') {
      steps{
        script {
          sh "docker-compose up -d --force-recreate --no-deps wordpress"
        }
      }
    }
  }
}